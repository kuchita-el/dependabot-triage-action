# Dependencies
node_modules/

# Test / coverage output
coverage/
*.lcov
.vitest/

# Build output（dist/ は VCS 管理外。self は triage.yml が実行時ビルド、
# 外部 consumer はリリース workflow がタグへ焼き込む。詳細は #47）
dist/

# Build cache
.tsbuildinfo
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# Local secrets (dotenv-style files)
.env*
!.env.example

# mise local state (mise.toml itself IS committed)
.mise.local.toml
